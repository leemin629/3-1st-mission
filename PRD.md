# PRD: 관광 데이터 Streamlit 대시보드

## 1. 프로젝트 개요
본 프로젝트는 관광 관련 데이터를 시각화하여 **연도별 방문자 수**, **지역별 지출 비중**, **업종별 소비 구성**을 한눈에 비교할 수 있는 Streamlit 기반 대시보드를 개발하는 것을 목표로 한다.

현재 **탭1은 완성된 상태**이며, 본 문서는 추가 개발 대상인 **탭2(지역별 비교)**와 **탭3(업종 구성 분석)**의 요구사항을 정의한다.

---

## 2. 프로젝트 목표
- 사용자가 **연도별 지역 관광 지표 변화**를 쉽게 파악할 수 있도록 한다.
- 지역별 **방문자 수(절대값)**와 **지출 비중(비율)**을 비교할 수 있도록 한다.
- 업종별 소비 비중을 시각화하여 **관광 소비 구조**를 직관적으로 이해할 수 있도록 한다.
- 초보자도 쉽게 사용할 수 있는 **직관적인 필터와 차트 중심 대시보드**를 제공한다.

---

## 3. 대상 사용자
- 관광 데이터 분석을 처음 접하는 학습자
- 지역별 관광 트렌드를 확인하려는 사용자
- 시각화를 통해 데이터 인사이트를 빠르게 파악하려는 일반 사용자

---

## 4. 범위

### 4.1 포함 범위
- Streamlit 기반 대시보드 UI
- Plotly 기반 인터랙티브 차트
- 연도 기준 시계열 비교
- 지역 필터 기능
- 업종별 소비 비중 시각화

### 4.2 제외 범위
- 사용자 로그인 기능
- 데이터베이스 연동
- 실시간 데이터 수집
- 고급 예측 분석 기능

---

## 5. 탭 구성

### 탭1. 개요 / 메인 시각화
- 현재 완성된 탭
- 기존 구현 내용 유지
- 본 PRD에서는 상세 수정 범위에 포함하지 않음

---

### 탭2. 지역별 비교

#### 5.1 목적
사용자가 **지역별 관광 지표를 연도 단위로 비교**할 수 있도록 한다.

#### 5.2 주요 기능

##### (1) 방문자 수 시각화
- 데이터: **방문자 수 (③)**
- 단위: **절대값**
- 대상 지역: **광역 17개 시·도**
- 차트 유형: **선 그래프 또는 막대 그래프**
- x축: **연도**
- y축: **방문자 수**
- 필터: **지역 선택**

##### (2) 지출 비중 시각화
- 데이터: **지출 비중 (④)**
- 단위: **백분율(%)**
- 차트 유형: **선 그래프 또는 막대 그래프**
- x축: **연도**
- y축: **지출 비율(%)**
- 필터: **지역 선택**

#### 5.3 사용자 인터랙션
- 사용자는 드롭다운 또는 멀티셀렉트로 **지역 선택** 가능
- 선택한 지역에 따라 차트가 **즉시 업데이트**되어야 함
- Hover 시 해당 연도의 **정확한 수치** 표시
- 범례를 통해 지역별 데이터 구분 가능

#### 5.4 화면 구성
- 상단: 탭 제목 및 간단한 설명
- 좌측 또는 상단: 지역 선택 필터
- 중앙:
  - 방문자 수 차트
  - 지출 비중 차트
- 필요 시 두 차트를 **세로 배치** 또는 **2열 배치** 가능

#### 5.5 기능 요구사항
- 최소 1개 이상의 지역을 선택할 수 있어야 함
- 연도 순으로 정렬된 데이터가 표시되어야 함
- 방문자 수와 지출 비중은 **서로 다른 차트**로 구분해야 함
- 비율 데이터는 `%` 형식으로 표시해야 함

#### 5.6 수용 기준
- 지역 선택 시 차트가 정상적으로 변경된다.
- x축은 항상 연도로 표시된다.
- 방문자 수는 절대값, 지출은 비율로 정확히 구분된다.
- 데이터 누락 시 에러 대신 안내 문구를 표시한다.

---

### 탭3. 업종 구성 분석

#### 6.1 목적
사용자가 관광소비가 **어떤 업종에 집중되는지**를 한눈에 파악할 수 있도록 한다.

#### 6.2 주요 기능

##### (1) 업종별 소비 비중 파이차트
- 데이터: **관광소비 업종 비중 (② 또는 ④ 기반)**
- 차트 유형: **파이차트**
- 표시 값: **업종별 비중(%)**
- 기준: **특정 연도 / 특정 지역 선택 가능**

#### 6.3 사용자 인터랙션
- 사용자는 **연도 선택** 가능
- 사용자는 **지역 선택** 가능
- 선택한 조건에 따라 파이차트가 **즉시 갱신**되어야 함
- Hover 시 업종명, 비중(%), 필요 시 실제 값 함께 표시

#### 6.4 화면 구성
- 상단: 탭 제목 및 설명
- 필터 영역:
  - 연도 선택
  - 지역 선택
- 메인 차트 영역:
  - 업종별 소비 비중 파이차트
- 필요 시 보조 표(Table) 추가 가능

#### 6.5 기능 요구사항
- 특정 연도와 지역 기준으로 업종 비중을 계산하여 표시해야 함
- 업종 비중의 합은 100% 또는 이에 근접해야 함
- 업종 수가 많을 경우 범례 또는 라벨이 겹치지 않도록 처리해야 함
- 데이터가 없을 경우 `"해당 조건의 데이터가 없습니다"` 안내 표시

#### 6.6 수용 기준
- 선택한 연도/지역에 따라 파이차트가 올바르게 갱신된다.
- 업종별 비중이 정확한 퍼센트로 표시된다.
- 범례와 색상 구분이 명확하다.
- 데이터가 없을 때 앱이 깨지지 않는다.

---

## 7. 데이터 요구사항

### 7.1 사용 데이터

#### ① 방문자 수 데이터
- 지역별
- 연도별
- 절대값

#### ② 지출 비중 데이터
- 지역별
- 연도별
- 비율(%)

#### ③ 업종별 소비 비중 데이터
- 업종별
- 연도별
- 지역별
- 비율(%)

### 7.2 전처리 요구사항
- 연도 컬럼은 숫자 또는 문자열로 일관성 있게 정리
- 지역명 표기 통일
- 비율 데이터는 `0~1`인지 `0~100`인지 확인 후 표시 형식 통일
- 결측치 처리 필요
- 차트 입력용 데이터프레임 구조 정리 필요

---

## 8. UI/UX 요구사항
- 탭 구조로 구분하여 직관적으로 이동 가능해야 함
- 필터는 사용자가 쉽게 찾을 수 있는 위치에 배치해야 함
- 색상은 지역/업종 구분이 명확해야 함
- 차트 제목, 축 라벨, 단위 표기를 포함해야 함
- 초보자도 이해할 수 있도록 간단한 설명 문구를 제공해야 함

### 예시 문구
- `"방문자 수는 절대값 기준입니다."`
- `"지출 비중은 지역별 소비 점유율(%)입니다."`

---

## 9. 기술 요구사항
- 프레임워크: **Streamlit**
- 시각화 라이브러리: **Plotly**
- 데이터 처리: **Pandas**

### 실행 방식
```bash
python -m streamlit run app.py
```

---

## 10. 예외 처리
- 데이터 파일 누락 시 안내 메시지 출력
- 선택한 지역/연도의 데이터가 없으면 차트 대신 안내 문구 표시
- Plotly 또는 필수 라이브러리 미설치 시 설치 안내 필요
- 비율 데이터 형식 오류 시 경고 메시지 또는 로그 출력

---

## 11. 성공 기준
- 탭2에서 지역별 방문자 수와 지출 비중을 비교 가능하다.
- 탭3에서 업종별 소비 비중을 파악 가능하다.
- 사용자가 필터를 변경해도 앱이 안정적으로 동작한다.
- 주요 차트가 직관적이며 수치 해석이 가능하다.

---

## 12. 개발 우선순위

### 1순위
- 탭2 방문자 수 차트
- 탭2 지출 비중 차트
- 지역 필터 연동

### 2순위
- 탭3 업종 비중 파이차트
- 연도/지역 필터 연동

### 3순위
- 안내 문구 개선
- 색상 및 레이아웃 고도화
- 보조 표 또는 요약 지표 추가

---

## 13. 예상 산출물
- Streamlit 대시보드 앱
- 탭1, 탭2, 탭3 구현본
- requirements.txt
- README 또는 프로젝트 설명 문서
- PRD 문서

---

## 14. 향후 작업 계획
1. `PRD.md` 파일 저장
2. Plotly 설치 (`python -m pip install plotly`)
3. Git 동기화 확인 (`git status`, `git pull`)
4. 탭2 구현
5. 탭3 구현
6. UI/예외 처리 보완